import { Callout, Steps } from "nextra/components";
import { Video } from "@components/video";
import { Bleed } from "nextra-theme-docs";

<Callout emoji="👾" type="info">
    Tested on macOS Sonoma 14.0{" "}
</Callout>

&nbsp;

<div align="center">
    <a href="https://www.python.org/">
        <img src="https://www.vectorlogo.zone/logos/python/python-icon.svg" alt="Python Logo" width="150" height="100" />

    </a>
    <p>Python Installation Guide</p>
    <p>Python is a general-purpose, high-level programming language.</p>

</div>


If you already install `Homebrew` on your macOS, Python is already installed while installing `Homebrew`. You can check it by typing `python3 --version{:sh}` in Terminal

```bash copy filename="sh"
python3 --version
```

### Running Python Code in the Terminal

Python is an interpreted language, which means you can run your Python code directly in the terminal without needing to compile it first. Here’s how you can get started.

#### Write Your First Python Program

Just type `python` or `python3` in the terminal and press `Enter` to open the Python interactive shell. In this shell, you can type Python commands directly and see the results immediately.

<Video src="https://raw.githubusercontent.com/ape-docs/kiwis.med/main/assets/docs/cs111/python_hello_world.mov" />

<Callout>
to exit the interactive shell, type `exit()` or press `Ctrl + D`.
</Callout>